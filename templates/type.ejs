<%# 
  ========================================================================
  TYPE TEMPLATE DOCUMENTATION
  ========================================================================
  
  Available Variables:
  
  1. code (string): 
     - Global Mode: All interfaces combined into one string.
     - Separate Mode: The code for a single interface.
     
  2. name (string | undefined):
     - Global Mode: undefined (or 'index')
     - Separate Mode: The name of the interface (e.g. 'UserDto')
     
  3. isGlobal (boolean):
     - true: Generating a single index.ts
     - false: Generating a specific type file
     
  4. config (Object): Global context
  ========================================================================
%>
<%# 这里可以放通用的文件头，比如 eslint-disable %>
// -------------------------------------------------------------------
// Generated by api-codegen-runner
// -------------------------------------------------------------------

<%- config.importTypeHeaderStr || '' %>

<%# 如果是分文件模式，可能需要在这里手动加一些通用 import，或者由底层库生成 %>
<%# 目前 api-codegen-universal 输出的 code 是纯接口定义 %>

<%- code %>